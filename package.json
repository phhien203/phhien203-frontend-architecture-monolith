{
  "name": "commerceOS",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/app-shell",
    "build": "tsc --noEmit && vite build",
    "lint:deps": "depcruise --config .dependency-cruiser.cjs src",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  }
}
